export class Messages {
    // userMessage: string;
    // botMessages: string[];
    // imgUrl: string;
    // createdAt: number;
    constructor(public userMessage: string, public botMessages: string[],
            public imgUrl: string, public createdAt: number) {
                
            }
}
